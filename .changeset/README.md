This folder stores release notes files for Changesets.

Create a changeset when you make a user-facing change:

```bash
npx changeset
```

The release workflow will open/update a version PR and publish to npm after merge.
